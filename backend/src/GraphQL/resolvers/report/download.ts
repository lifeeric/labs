import path from "path";
// import download from "download-file";

export default {
  downloadTemplates: async () => {
    const file = "../../../Helper/";

    const options = {
      directory: file,
      filename: "tests.ts",
    };

    // download()
  },
};
